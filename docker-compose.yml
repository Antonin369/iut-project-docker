services:
  app:
    build: .
    ports:
      - "80:8080"
    environment:
      APP_USER: "There"
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      - redis

  redis:
    image: redis:8
    volumes:
      - redis-data:/data

volumes:
  redis-data: